This project needs nodejs to run

Please install [nodejs](https://nodejs.org/en/)

Run the command below after to get all the packages 

```
npm install 
```

Run the command below to run the application with save detection
```
npm run start
```